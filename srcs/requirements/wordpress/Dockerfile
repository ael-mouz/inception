# Use a base image (Alpine or Debian)
FROM debian:stable

# Install WordPress and configure it here
# Ensure you set environment variables for WordPress configuration

# Expose the necessary ports if needed
# EXPOSE 80

# Start the WordPress service (e.g., php-fpm)
# CMD [command_to_start_php_fpm]
